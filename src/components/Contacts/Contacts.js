import PropTypes from 'prop-types';
import { Container, Section } from '../Common';

const Contacts = (props) => {
  return (
    <Section>
      <Container>
        <h4 style={{ color: 'red' }}>Contacts section</h4>
      </Container>
    </Section>
  );
};

Contacts.propTypes = {};

export default Contacts;
