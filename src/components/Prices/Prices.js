import PropTypes from 'prop-types';
import { Container, Section } from '../Common';

const Prices = (props) => {
  return (
    <Section>
      <Container>
        <h4 style={{ color: 'red' }}>Prices section</h4>
      </Container>
    </Section>
  );
};

Prices.propTypes = {};

export default Prices;
