import PropTypes from 'prop-types';
import { Container, Section } from '../Common';

const Reviews = (props) => {
  return (
    <Section>
      <Container>
        <h4 style={{ color: 'red' }}>Reviews section</h4>
      </Container>
    </Section>
  );
};

Reviews.propTypes = {};

export default Reviews;
