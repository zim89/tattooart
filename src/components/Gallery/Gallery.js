import PropTypes from 'prop-types';
import { Container, Section } from '../Common';

const Gallery = (props) => {
  return (
    <Section>
      <Container>
        <h4 style={{ color: 'red' }}>Gallery section</h4>
      </Container>
    </Section>
  );
};

Gallery.propTypes = {};

export default Gallery;
