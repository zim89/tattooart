import PropTypes from 'prop-types';
import { Container, Section } from '../Common';

const Care = (props) => {
  return (
    <Section>
      <Container>
        <h4 style={{ color: 'red' }}>Care section</h4>
      </Container>
    </Section>
  );
};

Care.propTypes = {};

export default Care;
